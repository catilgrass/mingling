[package]
name = "example-general-renderer"
version = "0.0.1"
edition = "2024"

[dependencies]
mingling = { path = "../../mingling", features = [
    "parser",
    "general_renderer",
    "json_serde_fmt",
    "yaml_serde_fmt",
] }
serde = { version = "1", features = ["derive"] }
